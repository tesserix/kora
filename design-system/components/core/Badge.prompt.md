Status/label pill. Semantic variants pull the muted status token pairs.

```jsx
<Badge>New</Badge>
<Badge variant="success">On track</Badge>
<Badge variant="warning">Over budget</Badge>
<Badge variant="outline">Draft</Badge>
```

Variants: default, secondary, destructive, outline, success, warning, error, info, neutral.
